corx {{ .Chart.AppVersion }} has been installed as `{{ .Release.Name }}` in
namespace `{{ .Release.Namespace }}`.

Verify the deployment is ready:

  kubectl --namespace {{ .Release.Namespace }} \
    rollout status deployment/{{ include "corx.fullname" . }}

{{- if .Values.ingress.enabled }}
Reach the proxy at any of the following hostnames:
{{- range .Values.ingress.hosts }}
  - http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}/
{{- end }}
{{- else if eq .Values.service.type "LoadBalancer" }}
The service was created with type=LoadBalancer. Wait for the cloud provider
to assign an external IP, then resolve it with:

  kubectl --namespace {{ .Release.Namespace }} get svc {{ include "corx.fullname" . }}
{{- else }}
Forward a port to talk to the proxy locally:

  kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "corx.fullname" . }} 8080:{{ .Values.service.port }}

  curl http://localhost:8080/livez
{{- end }}

To trigger a configuration reload without rolling pods, send SIGHUP:

  kubectl --namespace {{ .Release.Namespace }} \
    exec deploy/{{ include "corx.fullname" . }} -- kill -HUP 1

To inspect the active configuration that the binary is running with:

  kubectl --namespace {{ .Release.Namespace }} \
    exec deploy/{{ include "corx.fullname" . }} -- corx dump --format toml
